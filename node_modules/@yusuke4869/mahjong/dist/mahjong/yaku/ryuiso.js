"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ryuiso = void 0;
const array_1 = require("../hai/array");
const convert_1 = require("./convert");
const ryuiso = (data) => {
    const haiArrObj = (0, array_1.getHaiArrayObject)(data.hai.hai);
    for (const k of Object.keys(haiArrObj)) {
        for (const [index, value] of haiArrObj[k].entries()) {
            if (k === "manzu" || k === "pinzu") {
                if (value !== 0)
                    return convert_1.notExistsRes;
            }
            else if (k === "jihai") {
                if (index !== 5 && value !== 0)
                    return convert_1.notExistsRes;
                else
                    continue;
            }
            else if (index === 0 || index === 4 || index === 6 || index === 8) {
                if (value !== 0)
                    return convert_1.notExistsRes;
            }
        }
    }
    const res = {
        exists: true,
        yaku: "緑一色",
        han: 0,
        yakuman: 1,
    };
    return res;
};
exports.ryuiso = ryuiso;
//# sourceMappingURL=ryuiso.js.map